-- 仅更新仍使用上一版默认事件身份提示词的设置，管理员自定义内容不覆盖。
UPDATE "settings"
SET "value" = '【事件身份】只提取一件可被多篇报道共同指向的具体事实，填写直接主体、一个原子动作和一个辨识事项；没有明确具体事件时，主体输出 []，动作和事项留空。
主体、动作和事项必须来自标题或正文并保留计划/正式/完成等阶段；brand 只用于展示和搜索，不覆盖事件主体。
同一事件尽量使用一致表达；只输出短词或短语，不混入其他事件、不编造事实，也不要自行拼接 event_key，程序会据此生成事件键。'
WHERE "key" = 'ai_block_event_identity'
  AND instr("value", '【规范事件身份｜用于识别“同一件事的不同报道”】') = 1
  AND instr("value", 'event_action：只写一个 2-8 字原子动作词') > 0
  AND instr("value", '不自行拼接 event_key') > 0;
